const input = require('readline-sync');

function addUp(number) {
  let sum = 0;
  for (let i = 1; i <= number; i++) {
    sum += i;
  }
  return sum;
}

let bob = input.question("pick a number");

console.log(addUp(bob))